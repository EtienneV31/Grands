<h1>Coucou c'est les vieux</h1>

<% @activities.each do |activity| %>
  <p><%= activity.name %></p>
  <p><%= activity.description %></p>
  <p><%= activity.price %> €</p>
  <%= link_to 'Show', activity %>
<% end %>

<%= link_to 'New Activity', new_activity_path %>
